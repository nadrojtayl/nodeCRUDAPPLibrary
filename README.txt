1. TO DOS:

Change the ORM creator function so that if your example contains types that don't convert to mongo types, it gives you a verbose error message

2. Finish server side scripting to insert script tags for DB endpoints

3. Make scripr

4. Deal with foreign keys